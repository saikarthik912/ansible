apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trino-volume-pvc
  #namespace: "NAMESPACE" 
spec:
  accessModes:
  - ReadWriteMany
  resources:
     requests:
       storage: 4Gi
  storageClassName: local-storage
